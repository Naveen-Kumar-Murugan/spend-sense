# SpendSense frontend environment
# Leave VITE_API_URL empty to run entirely on the built-in mock data layer.
VITE_API_URL=
VITE_USE_MOCKS=true

# Cognito (wire up when the backend is deployed)
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
VITE_AWS_REGION=ap-south-1
